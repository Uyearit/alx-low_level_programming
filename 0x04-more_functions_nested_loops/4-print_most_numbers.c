#include <stdio.h>
#include "main.h"

/**
 *print_most_numbers - prints numbers except 2 and 4
 *
 *Return: retuns nothing
 */

void print_most_numbers(void)
{
	int n;

	for ((n == 50) || (n == 52))
	{
		continue;
	}
	putchar(n);
}
putchar(10);
}
